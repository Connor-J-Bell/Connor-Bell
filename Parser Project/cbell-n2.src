#Test for errors tokens entered after 'finish'. Should print 6 errors
#something is getting copied back wrong
prog AfterFinish
	start
		load (var1, var2);
		var1 := 5;
		var2 := 10;
		save (var1, var2)
	finish
error error
these are all errors
